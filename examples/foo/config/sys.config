[
  {foo, []},
  {mprom, [
  		{registry, default},
		{port, 4444}
	]},
  {kernel,
    [{logger,
      [{handler, default, logger_std_h,
        #{config => #{file => "log/erlang.log"}}}
      ]}]}
].
